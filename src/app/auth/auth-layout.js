import { Box } from '@mui/material';
import React from 'react';

const AuthLayout = ({ children }) => {
  return <Box className="auth-page-main-layout">{children}</Box>;
};

export default AuthLayout;
